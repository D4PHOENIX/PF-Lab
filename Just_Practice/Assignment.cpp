#include <iostream>
using namespace std;

const char STAR = '*'; 
const int PRIME = 71; 
int main(){
    int count, sum, newNum; 
    double x; 
    count = 1; 
    sum = count + PRIME; 
    x = 25.67; 
    newNum = count * 1 + 2; 
    sum = sum + count; 
    x = x + sum * count; 
    cout << " count = " << count << ", sum = " << sum << ", PRIME = " << PRIME << endl; 

return 0;
}

